# Rebuttal: Fresh Validation Limit

Gate6 is locked recombined stress replication. Gate6b/Gate6c show that fresh case39 full-solver validation is computationally expensive under the unmodified backend solver. Gate7 completes one fresh full-solver sanity bank; this supports directionality but is not an 8-bank statistical validation. Any fresh solver result remains a sanity check unless expanded into a full pre-registered validation set.
